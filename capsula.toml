[freeze]
# include-capsula-version = true
vault-directory = 'vault'

subdirectory-template = '%Y%m%d_%H%M%S'

include-cpu = false

pre-freeze-commands = [
    'poetry lock --check'
]

files = [
    'pyproject.toml',
    'poetry.lock',
]

git-repositories = [
    '',
]

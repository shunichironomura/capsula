[capture]
# include-capsula-version = true
vault-directory = 'vault'

capsule-template = '%Y%m%d_%H%M%S'

include-cpu = false

pre-capture-commands = [
    'poetry lock --check'
]

environment-variables = [
    'HOME',
]

[capture.files]
"pyproject.toml" = { hash = "sha256", copy = true }
"poetry.lock" = { hash = "sha256", copy = true }


[capture.git.repositories]
capsula = '.'

[monitor]
[monitor.files]

"examples/pi_cli.png" = { hash = "sha256" }

# [monitor.item.calculate-pi]
# optional = true

# [monitor.item.calculate-pi.files]
# "examples/pi_cli.png" = { hash = "sha256" }
